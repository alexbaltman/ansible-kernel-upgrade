---
# kernel.config can be generated by copying .config file for kernel 4.4.23 from an identical server.
# ansible-playbook upgrade.yml -i inventory.yml -u root -k

- hosts: all
  tasks:
    - assert:
        that:
          - ansible_version.major == 2
  sudo: yes
  roles:
    - kernel-upgrade
